---
import Container from "@components/container.astro";
import Faq from "@components/faq.astro";
import Features from "@components/features.astro";
import Hero from "@components/hero.astro";
import Layout from "@layouts/Layout.astro";
// Add new built-in i18n imports
import { getLangFromUrl, useTranslations } from "../../i18n/utils";

// Get current language and create translation function
const safeUrl = Astro.url || new URL('/', Astro.site || 'http://localhost:4321');
const lang = getLangFromUrl(safeUrl);
const t = useTranslations(lang);

// SEO/props: Matched to ui.ts structure (flat keys under language)
const title = t('site.title') ?? 'TikTokio - Scarica Video TikTok Online Gratuitamente Senza Watermark';
const description = t('site.description') ?? 'TikTokio – Il miglior scaricatore video TikTok online gratuito senza watermark per download TikTok illimitati. Scarica video TikTok con link in HD.';
const keywords = t('site.keywords') ?? 'TikTokio, scaricatore video TikTok online, scarica video TikTok, download TikTok illimitati, scaricatore TikTok gratuito, salva video TikTok, scaricatore TikTok online';
const image = 'meta/opengaph.webp';
const variant = 'TikTokio';

// Determine text direction based on language (add RTL languages as needed)
const dir = ['vi', 'ar', 'he', 'fa', 'ur'].includes(lang) ? 'rtl' : 'ltr';
---

<Layout title={title} description={description} keywords={keywords} image={image} favicon={favicon} variant={variant} dir={dir}>
  <Hero />
  <Container>
    <Features />
    <Faq />
  </Container>
</Layout>